import { PageHeader, Card, ui } from './ui'

// Stub — fleshed out in layer (5).
export function ProposalDetail() {
  return (
    <>
      <PageHeader title="Proposal" />
      <Card>
        <p style={ui.muted}>Proposal create / edit / view loads here.</p>
      </Card>
    </>
  )
}
